{
  "name": "voxel-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --dry",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "oxlint",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "prepublishOnly": "pnpm run typecheck && pnpm run test:coverage"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.0.18",
    "oxfmt": "0.35.0",
    "oxlint": "1.50.0",
    "typescript": "6.0.0-beta",
    "vitest": "4.0.18"
  },
  "pnpm": {
    "overrides": {}
  }
}
