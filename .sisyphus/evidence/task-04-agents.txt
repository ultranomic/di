Task 4: AGENTS.md Structure and Conventions
============================================

Scenario: AGENTS.md validates structure
Steps:
  1. grep -c "static readonly inject" AGENTS.md
  2. grep -c "decorator" AGENTS.md
  3. grep -c "MUST NOT" AGENTS.md

Result: PASS

Output:
  > grep -c "static readonly inject" AGENTS.md
  6

  > grep -c "decorator" AGENTS.md
  2

  > grep -c "MUST NOT" AGENTS.md
  1

Files Created:
  - /workspaces/di/AGENTS.md (274 lines)
  - /workspaces/di/.sisyphus/AGENTS.md (200 lines)

Content Verified:
  - Project Overview section present
  - Architecture section with Core Packages, DI Container, Module System, Controllers
  - Code Conventions with Service, Controller, Module patterns
  - Testing Strategy with TDD, vitest, 100% coverage
  - Commit Conventions with conventional commits
  - Guardrails section with MUST NOT and MUST lists
  - Common Patterns with Error Messages, Lifecycle Hooks, Request-Scoped Services
  - Project Structure diagram

  - .sisyphus/AGENTS.md includes:
    - Task Execution Patterns
    - Evidence File Conventions
    - Notepad Conventions
    - Verification Checklist
    - Common Commands
    - Package-Specific Notes
    - Error Message Guidelines
